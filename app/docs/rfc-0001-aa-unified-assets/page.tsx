import { Section } from "@/components/Section";

export default function RfcPage() {
  return (
    <Section title="RFC-0001: AA + Unified Assets (Placeholder)">
      <p className="lead">
        This placeholder route will host the canonical RFC for Ryvra account
        abstraction and unified asset handling.
      </p>
    </Section>
  );
}
